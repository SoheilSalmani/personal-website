---
title: Scala
relatedResources:
  - "9781617295270"
tags:
  - Scala
---

1. Test the Scala REPL.

   <Solution>

   ```scala fp=Test.scala nu
   println(1 + 2)
   ```

   ```scala
   scala> val a = "Hello, World!"
   val a: String = Hello, World!

   scala> :reset

   scala> :load Test.scala
   3

   scala> 1 + 2
   val res0: Int = 3

   scala> val x: Double = res0
   val x: Double = 3.0

   scala> def sayHello(n: String) = s"Hi $n!"
   def sayHello(n: String): String

   scala> sayHello("Scala")
   val res1: String = Hi Scala!

   scala> def sayHello(n: String): String = s"Hi $n!"
   def sayHello(n: String): String

   scala> class MyClass(n: String)
   // defined class MyClass

   scala> new MyClass("Martin")
   val res2: MyClass = MyClass@3e2c4d4b

   scala> object MyObject
   // defined object MyObject

   scala> MyObject
   val res3: MyObject.type = MyObject$@4fee14b

   scala> "na" * 3 + 42
   val res4: String = nanana42

   scala> :help
   ...

   scala> :quit
   ```

   </Solution>

2. Create a new Scala project with sbt, compile it, and run it.

   <Solution>

   ```sh
   sbt
   sbt:scala> new scala/hello-world.g8
   cd hello-world-template
   sbt
   sbt:scala> compile
   sbt:scala> run
   ```

   </Solution>

3. Add a new dependency, and an sbt plugin.

   <Solution>

   ```scala fp=build.sbt
   // ...

   libraryDependencies += "org.typelevel" %% "cats-core" % "2.3.0"

   // ...
   ```

   ```scala fp=project/plugins.sbt nu
   addSbtPlugin("org.scoverage" % "sbt-scoverage" % "1.6.1")
   ```

   </Solution>

4. Explain the structure of a typical Scala application built with sbt.

   <Solution>

   ![](/images/sbt-project-structure.png)

   </Solution>
